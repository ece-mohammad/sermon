# Sermon

Another serial monitor TUI using Python and textual, featuring custom send and receive sequences (hex and ascii), trigger actions on received sequence, save and load sessions, ASCII and hex display.

